using UnityEngine;

public class SingleBattleManager : MonoBehaviour
{
    [SerializeField]
    private PlayerBattleInfo playerBattleInfo;

    [SerializeField]
    private NPCBattleInfo npcBattleInfo;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
